## What is this?
This directory contains useful scripts for automation of menial tasks.

## Scripts
### `generate_default_match.py`
* This script generates a match-case statement from the common suffixes map (comment in src/gdobj/defaults.rs)